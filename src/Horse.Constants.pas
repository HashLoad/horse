unit Horse.Constants;

{$IF DEFINED(FPC)}
  {$MODE DELPHI}{$H+}
{$ENDIF}

interface

const
  DEFAULT_HOST = '0.0.0.0';
  DEFAULT_PORT = 9000;
  START_RUNNING = 'Server is runing on %s:%d';
  HORSE_VERSION = '3.1.6';

implementation

end.
